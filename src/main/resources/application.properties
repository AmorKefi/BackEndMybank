# adding database configuration
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:mybank}
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:090118/Shayma}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto = create

# adding transaction manager
spring.transaction.default-timeout=${DEFAULT_TIMEOUT:30}
spring.transaction.rollback-on-commit-failure=true